if(SLIDEIO_PROFILE_VSI)
    add_subdirectory(vsi_smoke)
    if(BUILD_TESTING)
        add_subdirectory(testlib)
        add_subdirectory(vsi)
        add_subdirectory(ometiff)
    endif()
else()
add_subdirectory(main)
add_subdirectory(ndpi)
add_subdirectory(vsi)
add_subdirectory(pke)
add_subdirectory(testlib)
add_subdirectory(converter)
add_subdirectory(transformer)
add_subdirectory(ometiff)
add_subdirectory(test_tools)
add_subdirectory(phtiff)
endif()
